{
  "name": "@ohah/hwpjs-server",
  "version": "0.1.0",
  "description": "HWP conversion API server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node"
  },
  "dependencies": {
    "@ohah/hwpjs": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
